# API base URL used by the browser.
# Keep /api when the frontend is served behind the same domain/reverse proxy.
VITE_API_URL=/api
# Local Vite proxy target used only during development.
VITE_DEV_API_TARGET=http://localhost:5000
