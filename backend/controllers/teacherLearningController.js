const pool = require("../config/db");
const getTeacherId = (req) => Number(req.user?.reference_id);
const getOrganizationId = (req) => Number(req.user?.organization_id);
const verifyClassTeacher = async (client, teacherId, organizationId, classId) => { const r=await client.query(`SELECT c.id,c.organization_id,c.name FROM classes c JOIN class_teachers ct ON ct.class_id=c.id WHERE c.id=$1 AND c.organization_id=$2 AND ct.teacher_id=$3`,[classId,organizationId,teacherId]); return r.rows[0]||null; };
const verifyClassSubject = async (client,classId,subjectId,organizationId)=>{const r=await client.query(`SELECT s.id,s.name FROM class_subjects cs JOIN subjects s ON s.id=cs.subject_id WHERE cs.class_id=$1 AND cs.subject_id=$2 AND s.organization_id=$3`,[classId,subjectId,organizationId]);return r.rows[0]||null;};
const createAssignment=async(req,res)=>{const c=await pool.connect();try{const t=getTeacherId(req),o=getOrganizationId(req),{class_id,subject_id,title,instructions,due_at,max_score,status}=req.body;if(!t||!o||!class_id||!subject_id||!title)return res.status(400).json({message:"Class, subject and assignment title are required."});await c.query("BEGIN");if(!await verifyClassTeacher(c,t,o,+class_id)){await c.query("ROLLBACK");return res.status(403).json({message:"You are not assigned to this class."});}if(!await verifyClassSubject(c,+class_id,+subject_id,o)){await c.query("ROLLBACK");return res.status(400).json({message:"This subject is not assigned to the selected class."});}const r=await c.query(`INSERT INTO assignments(organization_id,class_id,subject_id,teacher_id,title,instructions,due_at,max_score,status) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9) RETURNING *`,[o,+class_id,+subject_id,t,title.trim(),instructions||null,due_at||null,Number(max_score)||100,status||"published"]);await c.query("COMMIT");res.status(201).json(r.rows[0]);}catch(e){await c.query("ROLLBACK");console.error(e);res.status(500).json({message:"Failed to create assignment."});}finally{c.release();}};
const getMyAssignments=async(req,res)=>{try{const r=await pool.query(`SELECT a.*,c.name class_name,c.code class_code,s.name subject_name,COUNT(asub.id)::int submission_count FROM assignments a JOIN classes c ON c.id=a.class_id JOIN subjects s ON s.id=a.subject_id LEFT JOIN assignment_submissions asub ON asub.assignment_id=a.id WHERE a.teacher_id=$1 AND a.organization_id=$2 GROUP BY a.id,c.name,c.code,s.name ORDER BY a.created_at DESC`,[getTeacherId(req),getOrganizationId(req)]);res.json(r.rows);}catch(e){res.status(500).json({message:"Failed to load assignments."});}};
const getClassAssignments=async(req,res)=>{try{const t=getTeacherId(req),o=getOrganizationId(req),id=+req.params.classId;const a=await pool.query(`SELECT 1 FROM class_teachers ct JOIN classes c ON c.id=ct.class_id WHERE ct.teacher_id=$1 AND ct.class_id=$2 AND c.organization_id=$3`,[t,id,o]);if(!a.rows.length)return res.status(403).json({message:"You are not assigned to this class."});const r=await pool.query(`SELECT a.*,s.name subject_name,COUNT(asub.id)::int submission_count FROM assignments a JOIN subjects s ON s.id=a.subject_id LEFT JOIN assignment_submissions asub ON asub.assignment_id=a.id WHERE a.class_id=$1 AND a.organization_id=$2 GROUP BY a.id,s.name ORDER BY a.created_at DESC`,[id,o]);res.json(r.rows);}catch(e){res.status(500).json({message:"Failed to load class assignments."});}};
const createResource=async(req,res)=>{const c=await pool.connect();try{const t=getTeacherId(req),o=getOrganizationId(req),{class_id,subject_id,title,description,resource_url,resource_type}=req.body;if(!t||!o||!class_id||!title||!resource_url)return res.status(400).json({message:"Class, title and resource URL are required."});await c.query("BEGIN");if(!await verifyClassTeacher(c,t,o,+class_id)){await c.query("ROLLBACK");return res.status(403).json({message:"You are not assigned to this class."});}if(subject_id&&!await verifyClassSubject(c,+class_id,+subject_id,o)){await c.query("ROLLBACK");return res.status(400).json({message:"This subject is not assigned to the selected class."});}const r=await c.query(`INSERT INTO class_resources(organization_id,class_id,subject_id,teacher_id,title,description,resource_url,resource_type) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *`,[o,+class_id,subject_id?+subject_id:null,t,title.trim(),description||null,resource_url.trim(),resource_type||"link"]);await c.query("COMMIT");res.status(201).json(r.rows[0]);}catch(e){await c.query("ROLLBACK");res.status(500).json({message:"Failed to create resource."});}finally{c.release();}};
const getClassResources=async(req,res)=>{try{const t=getTeacherId(req),o=getOrganizationId(req),id=+req.params.classId;if(!await verifyClassTeacher(pool,t,o,id))return res.status(403).json({message:"You are not assigned to this class."});const r=await pool.query(`SELECT r.*,s.name subject_name,t.full_name teacher_name FROM class_resources r LEFT JOIN subjects s ON s.id=r.subject_id JOIN teachers t ON t.id=r.teacher_id WHERE r.class_id=$1 AND r.organization_id=$2 ORDER BY r.created_at DESC`,[id,o]);res.json(r.rows);}catch(e){res.status(500).json({message:"Failed to load resources."});}};
const createAnnouncement=async(req,res)=>{const c=await pool.connect();try{const t=getTeacherId(req),o=getOrganizationId(req),{class_id,title,body}=req.body;if(!t||!o||!class_id||!title||!body)return res.status(400).json({message:"Class, title and announcement body are required."});await c.query("BEGIN");if(!await verifyClassTeacher(c,t,o,+class_id)){await c.query("ROLLBACK");return res.status(403).json({message:"You are not assigned to this class."});}const r=await c.query(`INSERT INTO class_announcements(organization_id,class_id,teacher_id,title,body) VALUES($1,$2,$3,$4,$5) RETURNING *`,[o,+class_id,t,title.trim(),body.trim()]);await c.query("COMMIT");res.status(201).json(r.rows[0]);}catch(e){await c.query("ROLLBACK");res.status(500).json({message:"Failed to create announcement."});}finally{c.release();}};
const getClassAnnouncements=async(req,res)=>{try{const t=getTeacherId(req),o=getOrganizationId(req),id=+req.params.classId;if(!await verifyClassTeacher(pool,t,o,id))return res.status(403).json({message:"You are not assigned to this class."});const r=await pool.query(`SELECT a.*,t.full_name teacher_name FROM class_announcements a JOIN teachers t ON t.id=a.teacher_id WHERE a.class_id=$1 AND a.organization_id=$2 ORDER BY a.created_at DESC`,[id,o]);res.json(r.rows);}catch(e){res.status(500).json({message:"Failed to load announcements."});}};
const createLiveMeeting=async(req,res)=>{const c=await pool.connect();try{const t=getTeacherId(req),o=getOrganizationId(req),{class_id,subject_id,title,meeting_url,starts_at,ends_at}=req.body;if(!t||!o||!class_id||!title||!meeting_url||!starts_at)return res.status(400).json({message:"Class, title, meeting URL and start time are required."});await c.query("BEGIN");if(!await verifyClassTeacher(c,t,o,+class_id)){await c.query("ROLLBACK");return res.status(403).json({message:"You are not assigned to this class."});}if(subject_id&&!await verifyClassSubject(c,+class_id,+subject_id,o)){await c.query("ROLLBACK");return res.status(400).json({message:"This subject is not assigned to the selected class."});}const r=await c.query(`INSERT INTO live_meetings(organization_id,class_id,subject_id,teacher_id,title,meeting_url,starts_at,ends_at) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *`,[o,+class_id,subject_id?+subject_id:null,t,title.trim(),meeting_url.trim(),starts_at,ends_at||null]);await c.query("COMMIT");res.status(201).json(r.rows[0]);}catch(e){await c.query("ROLLBACK");res.status(500).json({message:"Failed to schedule live meeting."});}finally{c.release();}};
module.exports={createAssignment,getMyAssignments,getClassAssignments,createResource,getClassResources,createAnnouncement,getClassAnnouncements,createLiveMeeting};
